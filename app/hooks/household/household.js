import { useContext } from "react";

import HouseholdContext from "./context";

export default useHousehold = () => {
    const { notes } = useContext(HouseholdContext);

    const getNotes = () => {
        
    }
}